# ================================================================
# vast-stack .env — copy thành .env (hoặc điền trong Portainer
# "Environment variables" khi tạo stack). KHÔNG commit file này.
# ================================================================

# --- BẮT BUỘC ---
VAST_API_KEY=
API_LLM_SERVER=
MANAGEMENT_PASSWORD=

# --- Tuỳ chọn ---
TZ=Europe/Paris
VAST_PROVIDER_NAME=ln.vastai
VAST_CLUSTER_LABEL=cpa-vllm
VAST_INSTANCE_IDS=
VAST_MANAGE_ALL_INSTANCES=false

REMOTE_LLM_PORT=18000
TUNNEL_SCAN_SECONDS=5

AUTO_SYNC_INSTANCES=true
AUTO_SCALE_ENABLED=true
IDLE_TIMEOUT_MINUTES=30
BOOT_GRACE_SECONDS=900
BOOT_POLL_SECONDS=5
SYNC_INTERVAL_SECONDS=60
HEALTH_CACHE_SECONDS=3
LANGUAGE_PROMPT=Always communicate and explain in Vietnamese (Tiếng Việt). Keep code syntax, terminal commands, variable names, and programming technical terms in English.

# config-sync: bật DRY_RUN=true để chỉ log trước khi cho phép ghi thật
CONFIG_SYNC_DRY_RUN=false

# CPA plugin store (nếu cần cài plugin qua store)
CPA_PLUGIN_GITHUB_TOKEN=
CPA_CODEX_COMPACTION_KEY=

# --- LITELLM (audio gateway, chạy song song CPA) ---
# BẮT BUỘC (tự sinh, KHÔNG dùng key thật):
#   LITELLM_MASTER_KEY: python -c "import secrets; print('sk-litellm-'+secrets.token_hex(16))"
#     → vừa là password GUI /ui, vừa là key admin tạo virtual keys
#   LITELLM_DB_PASSWORD: python -c "import secrets; print(secrets.token_urlsafe(24))"
#     → chỉ là mật khẩu Postgres NỘI BỘ giữa 2 container, không phải API key
LITELLM_MASTER_KEY=
LITELLM_DB_PASSWORD=

# MỌI API key upstream (CPA delegate, OpenAI/ElevenLabs/Groq/Gemini/Deepgram
# cho audio) KHÔNG đặt ở đây — tạo trong GUI LiteLLM:
#   Settings → LLM Credentials, rồi Models + Endpoints → Add Model gắn credential.
# Key phát cho các client: Settings → Virtual Keys (mỗi bên 1 key, có budget).
